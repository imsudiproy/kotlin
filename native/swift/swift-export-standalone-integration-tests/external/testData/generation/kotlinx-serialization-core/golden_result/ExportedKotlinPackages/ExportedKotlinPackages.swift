public enum kotlin {
    public enum time {
    }
}
public enum kotlinx {
    public enum serialization {
        public enum builtins {
        }
        public enum descriptors {
        }
        public enum `internal` {
        }
        public enum modules {
        }
    }
}
